meta {
  name: Rename Movie (Command)
  type: http
  seq: 21
}

post {
  url: {{radarrUrl}}/api/v3/command
  body: json
  auth: none
}

body:json {
  {
    "name": "RenameMovie",
    "movieIds": [1422]
  }
}
