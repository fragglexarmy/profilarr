ComponentStatus:
  type: string
  enum:
    - healthy
    - degraded
    - unhealthy
  description: Individual component health status

HealthStatus:
  type: string
  enum:
    - healthy
    - degraded
    - unhealthy
  description: Overall health status
