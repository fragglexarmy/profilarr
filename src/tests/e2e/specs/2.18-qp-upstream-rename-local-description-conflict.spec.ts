/**
 * 2.18 Quality Profile — upstream rename plus local description conflict
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.18 QP upstream rename plus local description conflict', () => {
  test.todo('a) override');
  test.todo('b) align');
});
