/**
 * 2.19 Quality Profile — upstream rename plus local scoring conflict
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.19 QP upstream rename plus local scoring conflict', () => {
  test.todo('a) override');
  test.todo('b) align');
});
