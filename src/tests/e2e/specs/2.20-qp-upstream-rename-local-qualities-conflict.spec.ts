/**
 * 2.20 Quality Profile — upstream rename plus local qualities conflict
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.20 QP upstream rename plus local qualities conflict', () => {
  test.todo('a) override');
  test.todo('b) align');
});
