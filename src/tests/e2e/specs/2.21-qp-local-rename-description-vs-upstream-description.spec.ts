/**
 * 2.21 Quality Profile — local rename and description vs upstream description
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.21 QP local rename and description vs upstream description', () => {
  test.todo('a) override');
  test.todo('b) align');
});
