/**
 * 2.22 Quality Profile — local rename and scoring vs upstream scoring
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.22 QP local rename and scoring vs upstream scoring', () => {
  test.todo('a) override');
  test.todo('b) align');
});
