/**
 * 2.28 Quality Profile — local add quality group vs upstream reorder
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.28 QP local add quality group vs upstream reorder', () => {
  test.todo('a) override');
  test.todo('b) align');
});
