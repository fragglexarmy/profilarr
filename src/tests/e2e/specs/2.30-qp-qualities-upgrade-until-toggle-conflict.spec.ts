/**
 * 2.30 Quality Profile — local upgrade-until toggle vs upstream toggle
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.30 QP local upgrade-until toggle vs upstream toggle', () => {
  test.todo('a) override');
  test.todo('b) align');
});
