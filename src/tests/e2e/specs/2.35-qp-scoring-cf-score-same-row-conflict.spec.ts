/**
 * 2.35 Quality Profile — custom format score same row conflict
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.35 QP custom format score same row conflict', () => {
  test.todo('a) override');
  test.todo('b) align');
});
