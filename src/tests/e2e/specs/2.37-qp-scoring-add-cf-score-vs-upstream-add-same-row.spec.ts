/**
 * 2.37 Quality Profile — local add custom format score vs upstream add same row
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.37 QP local add custom format score vs upstream add same row', () => {
  test.todo('a) override');
  test.todo('b) align');
});
