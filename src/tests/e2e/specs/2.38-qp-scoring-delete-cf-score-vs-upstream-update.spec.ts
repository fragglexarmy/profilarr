/**
 * 2.38 Quality Profile — local delete custom format score vs upstream update
 *
 * Scaffold only: implement full e2e flow from docs/todo/conflict-testing.md.
 */
import { test } from '@playwright/test';

test.describe('2.38 QP local delete custom format score vs upstream update', () => {
  test.todo('a) override');
  test.todo('b) align');
});
